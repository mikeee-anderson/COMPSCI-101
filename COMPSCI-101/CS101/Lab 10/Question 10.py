def is_valid_password(password):
    if len(password) < 10:
        return False
    elif "123" in password or "abc" in password:
        return False
    alphabet = []
    numb = []
    for char in password
        if char.isalpha():
            alphabet += [char]
        elif char.isdigit():
            numb += [char]
    if len(alphabet) < 5:
        return False
    elif len(m)



